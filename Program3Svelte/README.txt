This is a Svelte web application, it can be run by first installing the
dependencies with `npm i` in this directory, and then by running `npm run dev`
and the URL used for the app will be shown in stdout.

Screenshots of the program in various states of the tests are in the Screenshots
directory!